packages:
    - name: core
      environment:
        ADMIN_USERNAME: "${ADMIN_USERNAME}"
        ADMIN_PASSWORD: "${ADMIN_PASSWORD}"
      functions:
        - name: login
          binary: false
          main: ""
          runtime: go:default
          web: true
        - name: json_response
          binary: true
          main: ""
          runtime: go:default
          web: true
