packages:
    - name: core
      environment:
        ADMIN_USERNAME: "${ADMIN_USERNAME}"
        ADMIN_PASSWORD: "${ADMIN_PASSWORD}"
        SECRET_KEY: "${SECRET_KEY}"
      functions:
        - name: login
          binary: true
          main: ""
          runtime: go:default
          web: true
